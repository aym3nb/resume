export interface Contact {
  email: string
  mobile: string
  github: string
}

export interface Experience {
  company: string
  location: string
  role: string
  period: string
  highlights: string[]
}

export interface Education {
  degree: string;
  institution: string;
  period: string;
  location: string;
}